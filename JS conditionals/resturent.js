const age = 66;
const  price = 5000;

if (age <= 12){
    console.log('You can eat it for free');
}
 else if(age >= 60) {
    // 15% discount for elderly people 
    const elderlyDiscount = price * 15 / 100;
    const elderlyPayAmount = price -discount;
    console.log('You will pay with discount', payAmount);
    const discount = price * 10 / 100;
    const payAmount = price - discount - elderlyDiscount;
    console.log 
} 
else{
    console.log('you will pay', price);
}